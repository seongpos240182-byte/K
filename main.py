import streamlit as st

# 페이지 기본 설정 (웹 브라우저 탭에 표시되는 제목과 아이콘)
st.set_page_config(
    page_title="K-콘텐츠 트렌드 대시보드",
    page_icon="🎬",
    layout="wide"
)

# ----------------------------------------------------
# 메인 표지 화면 구성
# ----------------------------------------------------
st.title("🎬 K-콘텐츠 글로벌 트렌드 데이터 대시보드")
st.markdown("---")

# 대시보드 소개 섹션
st.markdown("""
### 📊 대시보드 개요
본 시스템은 `k_contents_trend_data.csv` 파일을 기반으로 **전 세계 국가의 K-콘텐츠(드라마, 음악, 뷰티, 게임 등) 인기도 트렌드**를 다각도로 분석하기 위해 제작된 데이터 대시보드입니다.

제공되는 3가지 독립된 스크립트를 통해 원하는 인사이트를 도출할 수 있습니다.
""")

st.markdown("<br>", unsafe_allow_html=True) # 공백 추가

# 각 파일별 기능 안내 카드 (시각적 정돈)
col1, col2, col3 = st.columns(3)

with col1:
    st.info("""
    ### 1️⃣ 인기인기.py
    **장르별 인기도 분석**
    - 선택한 장르의 6개년 평균 인기도 확인
    - 전체 장르 간 인기도 비교 순위 차트 제공
    """)
    
with col2:
    st.success("""
    ### 2️⃣ 발전발전.py
    **국가별 성장 트렌드**
    - 전 세계 다양한 국가 선택 가능
    - 2020년~2025년 K-콘텐츠 인기도 연도별 선그래프
    """)
    
with col3:
    st.warning("""
    ### 3️⃣ 취향취향.py
    **국가별 선호도 분석**
    - 국가별 가장 인기 있는 '최애 장르' 자동 선정
    - 해당 선호 장르의 연도별 트렌드 변화 추적
    """)

st.markdown("---")
st.caption("⚙️ 실행 방법: 각 기능은 `streamlit run 인기인기.py` 형태로 개별 실행이 가능합니다.")
